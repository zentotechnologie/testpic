pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building..'
                checkout scm

            }
            stage('Setup & Build Ionic') {
                                                        echo "java -version"
                                                        java -version
                                }
        }
    }
}
